스팀림
버퍼를 가지고 순차적으로 이루어지는 입출력

자바의 입출력 스트림
응용 프로그램과 입출력 장치를 연결하는 소프트웨어 모듈


자바의 스트림 특징바이트 스트림과 문자스트림
    바이트 스트림 : 입출력 되는 데이터를 단순 바이트로 처리
    문자 스트림: 문자만 입출력하는 스트림

바이트 스크림 클래스
: 바이트 단위의 바이너리 값을 읽고 쓰는 스트림

바이트 스트림 클래스
inputStream / output stream
추상 클래스
바이트 스트림을 다루는 모든 클래스의 슈퍼 클래스
fileInputStream/FileOutputStream



pg 364
가변 크기의 문자열 저장 클래스

******Stringbuild/buffer를 사용하는 이유!!!/// String과의 차별점!

math 클래스를 화용ㅇ한 난수 발생
난수 발생: static double random()
java.util.random





pg389 9번
437 7번  이름 학점 -> HashMap 생성
정당3.1 나탈락2.4 최고조4.3 상당히 3.9 고득점4.0 put으로 추가
모든 학생 조사해 학점3.2이상 학생 이름 출력
추상클래스 5장 가장 중요하다.  